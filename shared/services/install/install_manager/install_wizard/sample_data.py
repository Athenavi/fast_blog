"""
安装向导 — 示例数据导入模块
"""
from pathlib import Path
from typing import Dict, Any


def import_sample_data(project_root: Path) -> Dict[str, Any]:
    """导入示例数据（占位功能）"""
    return {"success": True, "message": "示例数据导入功能待实现"}
