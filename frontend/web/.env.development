# 开发环境配置
# 开发服务器端口
VITE_PORT=5173

# 后端地址：dev 期的 /api 代理目标，同时供 SSR 阶段的服务端请求使用
VITE_PROXY_TARGET=http://localhost:9421
NUXT_PUBLIC_API_BASE_URL=http://localhost:9421

# 浏览器侧 WebSocket 基址（必须显式指定：dev 下不会经过 nitro/Vite 代理，见 nuxt.config.ts）
# 用 127.0.0.1 而不是 localhost —— Windows 上 localhost 先解析到 ::1，而后端只监听 IPv4
NUXT_PUBLIC_WS_BASE_URL=ws://127.0.0.1:9421
