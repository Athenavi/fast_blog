apiVersion: v1
kind: Service
metadata:
  name: { { include "fastblog.fullname" . } }
  labels:
    { { - include "fastblog.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector:
    { { - include "fastblog.selectorLabels" . | nindent 4 } }
